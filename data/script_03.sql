CREATE PROCEDURE `pa_check_usuario_nombre`(
	IN `pNombre` VARCHAR(150)
)
LANGUAGE SQL
NOT DETERMINISTIC
CONTAINS SQL
SQL SECURITY DEFINER
COMMENT ''
BEGIN

	SELECT id FROM usuario WHERE nombre = pNombre;

END